function which
    set cmd type --all (string escape -- $argv)
    echo $cmd
    echo "#########################"
    echo "## see also /bin/which ##"
    echo "#########################"
    eval $cmd
end
