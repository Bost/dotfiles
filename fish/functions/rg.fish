function rg
    # set cmd /usr/bin/rg --smart-case (string escape -- $argv)
    # echo $cmd
    # eval $cmd

    # --smart-case seems to be the default ripgprep setting
    # /usr/bin/rg (string escape -- $argv)
    /usr/bin/rg $argv
end
