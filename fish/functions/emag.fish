function emag
  echo "cd ~/dev/emacs-25; and git pull --rebase origin $argv; and git fetch --tags"
        cd ~/dev/emacs-25; and git pull --rebase origin $argv; and git fetch --tags
  lg # git lg-20
end
