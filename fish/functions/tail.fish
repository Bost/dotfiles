function tail --description "See https://www.brianstorti.com/stop-using-tail/"
    echo "# Note:"
    echo "# Instead of `/usr/bin/tail -f ...` try to use `less +F ...`"
    echo "# However `/usr/bin/tail -f ...` is better when watching multiple files at the same time"
end
