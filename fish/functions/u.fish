function u
    # echo sudo apt-get update; and sudo apt-get upgrade --yes; and sudo apt-get clean; and sudo checkrestart
    #      sudo apt-get update; and sudo apt-get upgrade --yes; and sudo apt-get clean; and sudo checkrestart
    echo sudo apt update; and sudo apt upgrade --yes; and sudo checkrestart
         sudo apt update; and sudo apt upgrade --yes; and sudo checkrestart

end
