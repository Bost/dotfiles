#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash -*-

## bash -n gxi
## shellcheck gxi

set -o xtrace
# -i, --install PACKAGE
guix package --install "$@"
{ retval="$?"; set +o xtrace; } 2>/dev/null
exit $retval
