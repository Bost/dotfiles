#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash -*-

## bash -n gxu
## shellcheck gxu

set -o xtrace
# -u, --upgrade[=REGEXP]
guix package --upgrade="$@"
{ retval="$?"; set +o xtrace; } 2>/dev/null
exit $retval
