#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash -*-

## bash -n gxse
## shellcheck gxse

set -o xtrace
# -s, --search=REGEXP; guix search
guix package --search="$@"
{ retval="$?"; set +o xtrace; } 2>/dev/null
exit $retval
