#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash -*-

## bash -n gxr
## shellcheck gxr

set -o xtrace
# -r, --remove PACKAGE
guix package --remove "$@"
{ retval="$?"; set +o xtrace; } 2>/dev/null
exit $retval
